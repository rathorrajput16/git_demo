#include <stdio.h>

int main() {
    int a = 5; // First number
    int b = 10; // Second number
    int sum = a + b; // Calculate sum

    // Print the result
    printf("The sum of %d and %d is %d\n", a, b, sum);

    return 0;
}

